# ユーザー操作フロー

## 概要

ユーザーがシミュレーターを使う流れと、各ステップで必要なUI要素を整理。

---

## 画面構成

```
┌─────────────────────────────────────────────────────┐
│  GitHub UI Guide                            [設定]  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           GitHub UI エリア                  │   │
│  │         （リポジトリ画面を再現）            │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │        ローカルファイルエリア               │   │
│  │       （clone後に出現）                     │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │              CLI エリア                     │   │
│  │          （コマンド入力）                   │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

## Step 0: シミュレーター起動

**ユーザーの操作:** ブラウザでページを開く

**画面の状態:**
- GitHub UI: 空リポジトリ画面（Quick setup、Clone URL）
- ローカルファイルエリア: 非表示
- CLI: ウェルカムメッセージ + 入力待ち

**必要な要素:**
- [ ] GitHub UI: 空リポジトリコンポーネント
- [ ] GitHub UI: リポジトリ名表示
- [ ] GitHub UI: Clone URL（コピーボタン付き）
- [ ] CLI: ウェルカムメッセージ
- [ ] CLI: コマンド入力欄

---

## Step 1: git clone

**ユーザーの操作:**
```
$ git clone https://github.com/user/my-project.git
```

**画面の変化:**
- CLI: 「Cloning into 'my-project'... done.」表示
- ローカルファイルエリア: 出現（空の状態）

**必要な要素:**
- [ ] CLI: git clone コマンド処理
- [ ] ローカルファイルエリア表示切り替え
- [ ] ファイル追加ボタン
- [ ] 状態: `isCloned: true`

---

## Step 2: ファイル追加/編集

**ユーザーの操作:**
- [+ ファイル追加] ボタン → ファイル名入力 → `untracked` で追加
- [編集] ボタン → ファイルが `modified` に変化

**ローカルファイルエリアの表示:**
```
📁 my-project/
├── README.md    [編集] ● modified
└── index.html   [編集] ○ untracked
```

**ファイル状態アイコン:**
| アイコン | 状態 | 色 |
|---------|------|-----|
| ○ | untracked | 緑 |
| ● | modified | 黄 |
| ◉ | staged | 青 |
| ✓ | committed | グレー |

**必要な要素:**
- [ ] ファイル追加モーダル/入力欄
- [ ] 各ファイルの[編集]ボタン
- [ ] ファイル状態の色分け表示

---

## Step 3: git status

**ユーザーの操作:**
```
$ git status
```

**CLI出力例:**
```
On branch main

Changes not staged for commit:
        modified:   README.md

Untracked files:
        index.html
```

**必要な要素:**
- [ ] CLI: git status コマンド処理
- [ ] 状態に応じた出力テキスト生成

---

## Step 4: git add

**ユーザーの操作:**
```
$ git add README.md
```
または
```
$ git add .
```

**画面の変化:**
- ファイル状態: `modified`/`untracked` → `staged`

**必要な要素:**
- [ ] CLI: git add コマンド処理
- [ ] 単一ファイル指定対応
- [ ] 全ファイル指定（.）対応
- [ ] ファイル状態更新

---

## Step 5: git commit

**ユーザーの操作:**
```
$ git commit -m "Add README"
```

**CLI出力:**
```
[main abc1234] Add README
 1 file changed
```

**画面の変化:**
- ファイル状態: `staged` → `committed`
- コミット履歴に追加

**必要な要素:**
- [ ] CLI: git commit コマンド処理
- [ ] -m オプションパース
- [ ] コミットハッシュ生成（ランダム7文字）
- [ ] コミット履歴追加

---

## Step 6: git push

**ユーザーの操作:**
```
$ git push
```

**CLI出力:**
```
Enumerating objects: 3, done.
Writing objects: 100% (3/3), done.
To https://github.com/user/my-project.git
   main -> main
```

**画面の変化:**
- **GitHub UI が更新！** 空 → ファイル一覧表示

**必要な要素:**
- [ ] CLI: git push コマンド処理
- [ ] GitHub UI: ファイル一覧コンポーネント
- [ ] GitHub UI: 各ファイルの最終コミットメッセージ
- [ ] 状態: `pushed: true`

---

## Step 7: git log

**ユーザーの操作:**
```
$ git log
```

**CLI出力:**
```
commit abc1234 (HEAD -> main)
    Add README

commit def5678
    Initial commit
```

**必要な要素:**
- [ ] CLI: git log コマンド処理
- [ ] コミット履歴の整形表示

---

## Step 8: git reset HEAD（add取り消し）

**ユーザーの操作:**
```
$ git reset HEAD README.md
```

**CLI出力:**
```
Unstaged changes after reset:
M       README.md
```

**画面の変化:**
- ファイル状態: `staged` → `modified`

**必要な要素:**
- [ ] CLI: git reset HEAD コマンド処理
- [ ] ファイル状態を元に戻す

---

## 補助コマンド

### help

**出力:**
```
利用可能なコマンド:
  git clone <url>      リポジトリをクローン
  git status           現在の状態を確認
  git add <file>       ファイルをステージング
  git add .            全ファイルをステージング
  git commit -m "msg"  コミットを作成
  git push             リモートに反映
  git log              コミット履歴を表示
  git reset HEAD <file> ステージングを取り消し
  help                 このヘルプを表示
  clear                画面をクリア
```

### clear

- CLI の出力履歴をクリア

---

## 状態管理まとめ

```javascript
const state = {
  // リポジトリ状態
  repo: {
    name: 'my-project',
    remoteUrl: 'https://github.com/user/my-project.git',
    isCloned: false,
    currentBranch: 'main'
  },

  // ファイル状態
  files: {
    'README.md': { status: 'committed' },
    'index.html': { status: 'untracked' }
  },

  // コミット履歴
  commits: [
    { hash: 'abc1234', message: 'Add README', files: ['README.md'] }
  ],

  // push済みコミット数（GitHub UIに反映済み）
  pushedCommitCount: 0
};
```

---

## UIコンポーネント一覧

| コンポーネント | ファイル | 説明 |
|--------------|---------|------|
| GitHubEmptyRepo | github-ui.js | 空リポジトリ画面 |
| GitHubFileList | github-ui.js | ファイル一覧画面 |
| LocalFiles | local-files.js | ローカルファイルエリア |
| CLI | cli.js | コマンド入力・出力 |
| FileAddModal | local-files.js | ファイル追加モーダル |
